# Mini-Shell
Developed a custom Unix Mini Shell in C that replicates core functionalities of a standard shell..Built-in commands such as cd, pwd, echo, and exit are handled internally, while external commands are executed using fork() and execvp().
